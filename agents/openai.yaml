interface:
  display_name: "Luna + Sol Workflow"
  short_description: "Explicit Luna execution and Sol reasoning workflow"
  default_prompt: "Use $luna-sol-workflow to complete this coding task with Luna as the sole executor and consult Sol only when escalation criteria are met."

policy:
  allow_implicit_invocation: false
